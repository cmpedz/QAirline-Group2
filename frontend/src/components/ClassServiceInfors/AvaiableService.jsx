import React from "react"
const AvaiableService = (props) => {
    return (
        <div>
            <p className="text-[0.9rem] md:text-[1rem]">{props.service}</p>
        </div>
    )
}

export default AvaiableService;